---
import Search from "./Search";
const allPosts = await Astro.glob("../../content/blog/*.{md,mdx}");
const posts = allPosts.filter((post) => !post.frontmatter.planned);
---

<Search client:load searchList={posts} />
